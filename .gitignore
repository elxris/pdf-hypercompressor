# Vendored wasm/JS/model assets — regenerate with ./vendor.sh (large, ~0.5 GB)
vendor/

# Python / OS cruft
__pycache__/
.DS_Store

# Terraform — local infra config for the xr.is zone (personal, not project-scoped)
terraform/

# R2 upload manifest (per-bucket sha256 cache for incremental deploys)
.r2-state/
